<Window x:Class="FileUploaderDemo.FileUploaderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileUploaderDemo.ViewModels"
        Title="File Uploader" Width="500" Height="150">
    <Window.DataContext>
        <local:FileUploaderViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Status:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock x:Name="uploadStatus" Grid.Column="1" Text="{Binding StatusMessage, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="2"/>
        <TextBlock Text="Progress:" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <ProgressBar x:Name="uploadProgress" Height="10" Grid.Row="1" Grid.Column="1" Value="{Binding Progress, Mode=OneWay}" Maximum="{Binding MaxProgress, Mode=OneWay}" VerticalAlignment="Center" Grid.ColumnSpan="2"/>
        <TextBox x:Name="filePath" Grid.Row="2" Grid.ColumnSpan="2" Height="20" IsEnabled="{Binding CanUpload}" Text="{Binding FilePath, Mode=TwoWay}" />
        <Button Content="Upload" Width="50" Grid.Row="2" Grid.Column="2" Height="20" Command="{Binding UploadCommand}" CommandParameter="{Binding Text, ElementName=filePath, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</Window>
